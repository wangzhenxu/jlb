<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AppIdentity">

	<select id="getIdentity" parameterType="long" resultType="com.timeloit.pojo.app.AppIdentity">
		select 
		foreign_nationality as foreignNationality,
		certificate_type as certificateType,
		certificate_code as certificateCode,
		certificate_pic as certificatePic,
		mobile_number as mobileNumber,
		name as name
		from app_identity
		where user_id=#{userId}
		limit 1
	</select>
	
</mapper>