<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AppLog">

	<!-- 得到所有日志 -->
	<select id="getAppLogs" parameterType="long" resultType="com.timeloit.pojo.app.AppLog">
		select 
		al_id as alId,
		app_id as appId,
		operator as operator,
		operate_time as operateTime,
		action as action,
		action_type as actionType,
		status as status,
		comments as comments,
		refuse_code as refuseCode 
		from app_log
		where app_id=#{appId}
		order by operate_time 
	</select>
	<!-- 分页得到应用日志 -->
	<select id="getAppLogsPage" parameterType="java.util.Map" resultType="com.timeloit.pojo.app.AppLog">
		select 
		al_id as alId,
		app_id as appId,
		operator as operator,
		operate_time as operateTime,
		action as action,
		action_type as actionType,
		status as status,
		comments as comments,
		refuse_code as refuseCode 
		from app_log
		where app_id=#{appId}
		order by operate_time 
		limit #{skipResults},#{maxResults}
	</select>
	<!-- 查询应用操作日志总数 -->
	<select id="selectAppLogsCount" parameterType="long" resultType="Integer">
		select count(*)
		from app_log 
		where app_id=#{appId}
	</select>
</mapper>