<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AppMaintenanceLog">

	<select id="getAppMaintenanceLogs" parameterType="long" resultType="com.timeloit.pojo.app.AppMaintenanceLog">
		select 
		maintenance_notices as maintenanceNotices,
		maintenance_reason as maintenanceReason,
		maintenance_time as maintenanceTime
		from app_maintenance_log
		where app_id=#{appId}
		order by maintenance_time
	</select>
		<!-- 分页得到应用日志 -->
	<select id="getAppMaintenanceLogsPage" parameterType="java.util.Map" resultType="com.timeloit.pojo.app.AppMaintenanceLog">
		select 
		maintenance_notices as maintenanceNotices,
		maintenance_reason as maintenanceReason,
		maintenance_time as maintenanceTime
		from app_maintenance_log
		where app_id=#{appId}
		order by maintenance_time
		limit #{skipResults},#{maxResults}
	</select>
	<!-- 查询应用操作日志总数 -->
	<select id="AppMaintenanceLogsCount" parameterType="long" resultType="Integer">
		select count(*)
		from app_maintenance_log 
		where app_id=#{appId}
	</select>
</mapper>